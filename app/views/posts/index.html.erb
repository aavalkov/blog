
<% @posts.sort.reverse.each do |post| %>
  <dt><h3><%= link_to "#{post.name}", post_path(post) %></h3></dt>
  <dl><%= post.article %></dl>
<% end %>

<% if current_user != nil %>
  <%= link_to "New post", new_post_path, :class => 'btn btn-success' %>
<% end %>


